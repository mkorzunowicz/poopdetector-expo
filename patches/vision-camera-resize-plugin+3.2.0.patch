diff --git a/node_modules/vision-camera-resize-plugin/android/src/main/cpp/ResizePlugin.cpp b/node_modules/vision-camera-resize-plugin/android/src/main/cpp/ResizePlugin.cpp
index a547d27..27054bc 100644
--- a/node_modules/vision-camera-resize-plugin/android/src/main/cpp/ResizePlugin.cpp
+++ b/node_modules/vision-camera-resize-plugin/android/src/main/cpp/ResizePlugin.cpp
@@ -385,7 +385,9 @@ FrameBuffer ResizePlugin::convertBufferToDataType(const FrameBuffer& frameBuffer
       return frameBuffer;
     case FLOAT32: {
       float* floatData = reinterpret_cast<float*>(destination.data());
-      status = libyuv::ByteToFloat(frameBuffer.data(), floatData, 1.0f / 255.0f, size);
+      // Use 1.0f scale to keep values in [0-255] range (unnormalized)
+      // Change to 1.0f / 255.0f if normalized [0-1] values are needed
+      status = libyuv::ByteToFloat(frameBuffer.data(), floatData, 1.0f, size);
       break;
     }
   }
diff --git a/node_modules/vision-camera-resize-plugin/ios/ResizePlugin.mm b/node_modules/vision-camera-resize-plugin/ios/ResizePlugin.mm
index f52d20f..471278f 100644
--- a/node_modules/vision-camera-resize-plugin/ios/ResizePlugin.mm
+++ b/node_modules/vision-camera-resize-plugin/ios/ResizePlugin.mm
@@ -373,7 +373,9 @@ - (FrameBuffer*)convertInt8Buffer:(FrameBuffer*)buffer toDataType:(ConvertDataTy
       uint8_t* input = (uint8_t*)source->data;
       float* output = (float*)destination->data;
       size_t numBytes = source->height * source->rowBytes;
-      float scale = 1.0f / 255.0f;
+      // Use 1.0f scale to keep values in [0-255] range (unnormalized)
+      // Change to 1.0f / 255.0f if normalized [0-1] values are needed
+      float scale = 1.0f;
 
       vDSP_vfltu8(input, 1, output, 1, numBytes);
       vDSP_vsmul(output, 1, &scale, output, 1, numBytes);
